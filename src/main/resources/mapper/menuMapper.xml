<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 
Windows -> Preferences -> xml -> xml(Wild Web Developer) -> 체크
namespace : com.green.menus.mapper.MenuMapper 인터페이스 위치 
-->
<mapper namespace="com.green.menus.mapper.MenuMapper">
	
<!-- 메뉴목록 조회 -->
	<select id="getMenuList"
	        resultType="com.green.menus.dto.MenuDTO">
	SELECT     MENU_ID, MENU_NAME, MENU_SEQ
	 FROM      MENUS
	 ORDER BY  MENU_SEQ ASC
	</select>
	
<!-- 메뉴한개 조회 -->	
	<select id="getMenu">
	SELECT     MENU_ID, MENU_NAME, MENU_SEQ
	 FROM      MENUS
	 WHERE     MENU_ID = #{ menu_id }
	</select>
    
<!-- 현재 게시판 -->
    <select id="getMenu_name" >
	SELECT MENU_NAME
	FROM   MENUS  
	WHERE  MENU_ID = #{menu_id}
    </select>
    
    <!-- 메뉴추가 조회 -->
    <insert id="insertMenu">
    INSERT INTO MENUS ( MENU_ID, MENU_NAME, MENU_SEQ)
     VALUES ( #{menu_id}, #{menu_name}, #{menu_seq} )
    </insert>
    
    <!-- 메뉴삭제 조회 -->
    <delete id="deleteMenu">
    DELETE FROM MENUS 
    WHERE MENU_ID = #{menu_id}
    </delete>
    
    <!-- 메뉴수정 조회 -->
    <update id="updateMenu">
    UPDATE MENUS
     SET   MENU_NAME = #{menu_name},
           MENU_SEQ  = #{menu_seq}
     WHERE MENU_id   = #{menu_id}
    </update>
    
    <insert id="insertMenu2">
    INSERT INTO MENUS (
    MENU_ID,
    MENU_NAME,
    MENU_SEQ
	) VALUES (
    	(SELECT 'MENU' || 
            TRIM( TO_CHAR( NVL( MAX( SUBSTR(menu_id,5,2) ), 0 ) + 1, '00') )
     	 FROM MENUS),
    	#{menu_name},
    	(SELECT NVL( MAX(MENU_SEQ), 0) + 1 
     	 FROM MENUS)
	)
    </insert>
</mapper>
